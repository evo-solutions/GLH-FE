# EvoSales — Layout Base

Next.js + Ant Design + Tailwind + next-intl + pharma theme.

```bash
npm install && npm run dev
```

## Theme

- CSS: `globals.css` (`:root` / `.dark`) + Tailwind (`text-pharma`, `bg-background`, …)
- Ant Design: `libs/antd/AntdProvider.tsx` ← `libs/theme/colors.ts`
